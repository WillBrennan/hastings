#include <gtest/gtest.h>

#include "hastings/libinfer/ops.h"
#include "hastings/libinfer/tensor.h"

TEST(LibInferOp, preprocess) { FAIL(); }

TEST(LibInferOp, sigmoid) { FAIL(); }

TEST(LibInferOp, colorByProbs) { FAIL(); }

TEST(LibInferOp, peakFinding) {
    using libinfer::Device;
    using libinfer::Ordering;
    using libinfer::peakFinding;
    using libinfer::Shape;
    using libinfer::Tensor;
}

TEST(LibInferOp, objectsAsPoints) { FAIL(); }